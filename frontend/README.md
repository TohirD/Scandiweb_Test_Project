code to run backend: 
1) cd backend
2) composer install
3) php -S localhost:8000 -t public

code to run frontend:
1) npm install or yarn
2) npm run dev or yarn dev

Note: before run frontend check that backend is workig and then run frontend!